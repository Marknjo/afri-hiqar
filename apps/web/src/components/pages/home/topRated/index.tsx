import Wrapper from '@layouts/Wrapper'

export default function TopRated({ className }: { className?: string }) {
  return (
    <Wrapper className={className}>
      <p>Top Rated Tours</p>
    </Wrapper>
  )
}
